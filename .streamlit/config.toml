[server]
# Required for hosting behind a reverse proxy (Render, nginx, etc.)
headless = true
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false

[theme]
# LinkedIn-inspired professional light theme
# Streamlit base tokens — fine-grained overrides live in utils/ui_components.py CSS
base                     = "light"
primaryColor             = "#0A66C2"
backgroundColor          = "#F3F2EE"
secondaryBackgroundColor = "#FFFFFF"
textColor                = "#0F172A"
font                     = "sans serif"
